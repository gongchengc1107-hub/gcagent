---
description: 项目的编码规范，包括 TypeScript 使用要求、命名约定（变量、常量、接口、组件等）、业务函数命名规则。当编写或审查代码时读取此规则。
alwaysApply: false
---

<rule>
# 编码规范

## TypeScript 规范

- 所有代码必须使用 TypeScript，禁止使用 JavaScript
- 所有 `.ts` 和 `.tsx` 文件必须包含完整的类型定义
- 禁止使用 `any` 类型（除非有明确理由并添加注释说明）
- 接口、类型定义必须使用 PascalCase 命名
- 组件 Props 必须定义清晰的 TypeScript 接口
- **类型定义优先使用 `type`，而非 `interface`**（除非需要 declaration merging 或 extends 继承）

```typescript
// ✅ 推荐 - 使用 type
type UserInfo = {
  id: string;
  name: string;
};

type ButtonProps = {
  onClick: () => void;
  disabled?: boolean;
};

// ✅ 可以 - 需要继承时使用 interface
interface BaseEntity {
  id: string;
  createdAt: string;
}

interface User extends BaseEntity {
  name: string;
}

// ❌ 不推荐 - 简单类型定义使用 interface
interface UserInfo {
  id: string;
  name: string;
}
```

> **原因**：`type` 更灵活，支持联合类型、交叉类型、映射类型等高级特性，语法更简洁统一。`interface` 仅在需要 declaration merging 或 class implements 时使用。

## React 导入规范（NON-NEGOTIABLE）

**禁止默认导入整个 React 命名空间，必须按需具名导入。**

```typescript
// ✅ 正确 - 按需具名导入
import { FC, useState, useRef, useCallback } from 'react';

// ❌ 错误 - 导入整个 React 对象
import React from 'react';
import React, { useState } from 'react';
```

**禁止在业务代码中出现 `React.xxx` 形式的调用**，包括但不限于：

| 禁止写法 | 正确替代 |
|---------|---------|
| `React.FC` | `FC`（从 react 按需导入） |
| `React.useState` | `useState` |
| `React.useEffect` | `useEffect` |
| `React.useRef` | `useRef` |
| `React.ReactNode` | `ReactNode` |
| `React.ChangeEvent<T>` | `ChangeEvent<T>` |
| `React.MouseEvent` | `MouseEvent` |
| `React.CSSProperties` | `CSSProperties` |

> **原因**：项目使用 React 17+ 的新 JSX Transform，`.tsx` 文件无需 React 在作用域内；显式导入 React 对象会造成无意义的引用冗余，且与团队 lint 规则冲突。

## 类型导入规范（NON-NEGOTIABLE）

**`import type` 必须与普通 `import` 分开书写，禁止混合在同一条导入语句中。**

```typescript
// ✅ 正确 - 类型导入单独一行
import { useState, useEffect } from 'react';
import type { FC, ReactNode, CSSProperties } from 'react';

import { Button, Modal } from '@bedrock/components';
import type { ButtonProps } from '@bedrock/components';

// ❌ 错误 - 类型与普通导入混在一起
import { useState, type FC, useEffect } from 'react';
import { Button, type ButtonProps } from '@bedrock/components';
```

> **原因**：分离类型导入可以让编译器更好地进行 Tree-shaking（类型导入在编译后会被完全擦除），同时提高导入语句的可读性，明确区分值依赖和类型依赖。

## 导入正确性校验（NON-NEGOTIABLE）

每次新增或修改 `import` 语句后，必须逐条确认：

1. **符号存在性**：导入的名称在目标模块中确实存在（如 `ColumnType` 而非 `ColumnProps`）
2. **导出路径**：路径正确（参考项目已有的同类 import，用 `grep_search` 搜索确认）
3. **包已安装**：第三方包在 `package.json` 中存在（如 `@ahooksjs/use-url-state` 需独立安装，不能从 `ahooks` 导入）
4. **禁止 `React.xxx`**：此条已在「React 导入规范」中声明；此处再次强调——**写完代码后必须全局搜索 `React.` 并替换为具名导入**，这是自查清单的一部分

> 自查方法：代码编写完成后，对新增/修改的文件执行 `grep_search` 检查 `React\.` 和所有新增的 import 符号名。

5. **完整性校验（NON-NEGOTIABLE）**：每次任务结束（代码生成、重构、撤回等操作完成后），必须检查所有被修改文件的 import 区域：
   - **缺失导入**：文件中使用了但未 import 的组件/函数/类型（尤其是撤回操作后 import 可能被误删）
   - **多余导入**：文件中 import 了但代码中不再使用的符号（尤其是提取/内联组件后残留的旧 import）
   - 检查方法：对修改文件中所有 import 的符号，在文件内搜索确认是否有使用；对文件中使用的外部符号，确认是否有对应 import

## 命名规范

| 类型 | 命名规则 | 示例 |
|------|----------|------|
| 文件夹/路由 | kebab-case | `user-profile`、`login` |
| 变量/函数 | camelCase | `userList`、`onSubmit` |
| 常量 | UPPER_CASE | `API_TIMEOUT` |
| 枚举及属性 | PascalCase / UPPER_CASE | `ApiMethod.GET` |
| 接口/类 | PascalCase | `UserInfo`、`BaseResponse` |
| React 组件 | PascalCase | `UserProfile` |
| 自定义 Hooks | use 开头 | `useUserStore` |
| 布尔值变量 | is/has/should/can 前缀 | `isLoading`、`hasPermission`、`shouldRefresh` |
| 页面组件导出 | `Index` | `const Index: FC = () => {}; export default Index;` |

## 业务函数命名

- **事件处理函数**：`onXxx`（如 `onBannerClick`、`onFormSubmit`）
- **内部处理函数**：`handleXxx`（如 `handleDelete`、`handleSubmit`）

## 安全编码规范

- 预判所有数据可能为 `null`/`undefined`，使用 `?.` 可选链 + `??` 提供默认值
- lodash **必须按需引入**（`import { map, filter } from 'lodash'`），禁止 `import _ from 'lodash'` 全量导入
- 对数组解构赋值时提供默认值：`const [list = []] = data ?? []`
- 方法调用也需要可选链：`window.parent?.postMessage?.(data, url)`

## 接口类型复用规范（NON-NEGOTIABLE）

**后端接口请求/响应类型定义统一从 `@/OpenApiServices/museService/api.ts` 导入，禁止在业务代码中重复定义。**

```typescript
// ✅ 正确 - 直接使用已生成的类型
import type { AssetThirdPartyOperationProcessReq } from '@/OpenApiServices/museService/api';

const requestData: AssetThirdPartyOperationProcessReq = {
  attachments: [...],
  gen_tpose: true,
};

// ❌ 错误 - 自己重新定义接口参数类型
interface FourViewGenerationParams {
  attachments: { attachment_id: string; view_type: string }[];
  gen_tpose: boolean;
  // ...
}
```

> **原因**：`api.ts` 由 `pnpm api:genapi` 根据后端 Swagger 文档自动生成，类型定义与后端保持同步。自定义类型会导致：
> 1. 字段名不一致（驼峰 vs 下划线）
> 2. 字段缺失或冗余
> 3. 后端接口变更时无法自动同步

**如需扩展**，可使用 TypeScript 工具类型：

```typescript
// 扩展已有类型
type ExtendedReq = AssetThirdPartyOperationProcessReq & { customField: string };

// 选取部分字段
type PartialReq = Pick<AssetThirdPartyOperationProcessReq, 'attachments' | 'gen_tpose'>;
```

## 功能合并规范（NON-NEGOTIABLE）

**功能类似的函数/方法应合并为一个，通过参数控制不同行为，禁止定义多个高度相似的函数。**

```typescript
// ✅ 正确 - 通过参数控制不同行为
interface SubmitGenerationParams {
  operationType: ThirdPartyOperationType;
  data: AssetThirdPartyOperationProcessReq;
  /** 资源包来源时传入 */
  engineeringParams?: {
    assetId: string;
    organization: string;
  };
}

const submitGeneration = ({ operationType, data, engineeringParams }: SubmitGenerationParams) => {
  const apiCall = engineeringParams
    ? apiWithAssetId({ ...engineeringParams, operationType }, data)
    : apiWithoutAssetId({ operationType }, data);
  return apiCall.then(handleResponse);
};

// ❌ 错误 - 定义两个高度相似的函数
const submitGeneration = (operationType, data) => { /* ... */ };
const submitGenerationFromEngineering = (operationType, assetId, organization, data) => { /* ... */ };
```

> **原因**：
> 1. 减少重复代码，便于维护
> 2. 调用方逻辑更简洁，无需判断调用哪个函数
> 3. 错误处理、日志记录等通用逻辑只需写一次
</rule>
