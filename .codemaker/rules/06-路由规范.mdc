---
description: 项目路由配置规范，包括 routes.ts 集中配置、hash 路由模式、路由权限配置。当需要新增或修改路由时读取此规则。
alwaysApply: false
---

<rule>
# 路由规范

## 路由基本约定

- 路由使用 **hash 模式**（URL 形如 `/#/path/to/page`）
- 路由表集中配置在**项目根目录 `routes.ts`**（非 `src` 内）
- 禁止使用 UmiJS 约定式路由（文件即路由）——本项目所有路由均集中声明
- **禁止**直接使用 React Router 的 `<Route>`、`<Switch>` 等组件

## 路由配置示例

```typescript
// routes.ts（根目录）
import type { Route } from '@umijs/max';

const routes: Route[] = [
  {
    path: '/',
    component: '@/layouts/BasicLayout',
    routes: [
      {
        path: '/muse/featured',
        component: '@/pages/Featured',
      },
      {
        path: '/:org/model',
        component: '@/pages/Model',
      },
    ],
  },
];

export default routes;
```

## 路由权限控制

页面级权限判断在**页面组件内部**通过 Redux state 实现：

```typescript
import { useTypedSelector } from '@/redux/useTypedSelector';

const MyPage: React.FC = () => {
  const { isAdmin, systemManagement } = useTypedSelector((state) => state.account);
  const { permissions } = useTypedSelector((state) => state.organization);

  // 无权限时渲染提示或重定向
  if (!isAdmin && !permissions?.view) {
    return <NoPermission />;
  }

  return <PageContent />;
};
```

权限来源详见 `muse-rule.mdc` 第 4 节「权限系统规范」。

## 路由跳转

```typescript
import { history, useNavigate } from '@umijs/max';

// 命令式跳转
history.push('/muse/featured');
history.replace('/login');

// 在组件中使用 hooks
const navigate = useNavigate();
navigate('/muse/featured');
navigate(-1);  // 后退
```

## 禁止事项

- **禁止**直接使用 `window.location.href = '...'` 进行路由跳转（SSO 跳转除外）
- **禁止**在 `routes.ts` 之外手动注册路由
</rule>
