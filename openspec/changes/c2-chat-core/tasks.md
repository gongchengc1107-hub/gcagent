## 1. 会话管理侧边栏

- [ ] 1.1 创建 Chat 页面布局（左侧会话列表面板 260px + 右侧对话面板）
- [ ] 1.2 实现 SessionList 组件（会话列表，支持滚动）
- [ ] 1.3 实现会话时间分组逻辑（置顶 / 今天 / 昨天 / 本周 / 更早）
- [ ] 1.4 实现新建会话功能（顶部"新建"按钮，创建后自动切换）
- [ ] 1.5 实现会话切换（点���列表项切换当前会话，高亮选中项）
- [ ] 1.6 实现会话重命名（双击标题内联编辑，Enter 确认 / Esc 取消）
- [ ] 1.7 实现会话删除（确认弹窗 → 删除会话和消息 → 自动切换下一个）
- [ ] 1.8 实现会话置顶/取消置顶
- [ ] 1.9 实现会话导出（Markdown / JSON 两种格式，触发文件下载）
- [ ] 1.10 实现自定义 ContextMenu 右键菜单组件（置顶/重命名/导出/删除）

## 2. 会话搜索

- [ ] 2.1 实现 SearchModal 搜索弹窗组件（Ant Design Modal，输入框自动聚焦）
- [ ] 2.2 监听全局 ⌘K 快捷键唤起搜索弹窗
- [ ] 2.3 实现标题模糊搜索逻辑 + 搜索结果列表
- [ ] 2.4 实现点击搜索结果切换会话并关闭弹窗

## 3. 对话输入区

- [ ] 3.1 创建 MessageInput 组件（多行 textarea，底部固定）
- [ ] 3.2 实现 Enter 发送 / Shift+Enter 换行 / 空内容禁止发送
- [ ] 3.3 实现 Agent 选择下拉（显示所有 Agent，默认 Sisyphus）
- [ ] 3.4 实现模型选择下拉（20+ 模型，按厂商分组）
- [ ] 3.5 定义模型列表常量（Anthropic / OpenAI / Google / DeepSeek / Moonshot / Alibaba）
- [ ] 3.6 实现 Token 用量百分比指示器
- [ ] 3.7 实现 AI 回复中发送限制（输入框置灰 + 发送按钮禁用）

## 4. 消息展示区

- [ ] 4.1 创建 MessageList 组件（消息列表容器，支持滚动）
- [ ] 4.2 创建 MessageBubble 组件（区分用户消息 / AI 消息样式）
- [ ] 4.3 实现 Markdown 渲染（react-markdown + remark-gfm + rehype-highlight）
- [ ] 4.4 实现代码块组件（语法高亮 + 语言标签 + 一键复制按钮）
- [ ] 4.5 实现消息时间戳显示
- [ ] 4.6 实现空会话欢迎引导界面（Logo + 提示文案 + 建议提问示例）

## 5. 流式输出（Mock）

- [ ] 5.1 实现 Mock SSE 流式输出服务（setInterval 逐字追加，30ms/字符）
- [ ] 5.2 准备 Mock 回复内容（含 Markdown 标题、列表、代码块、表格，验证渲染能力）
- [ ] 5.3 实现流式输出期间自动滚动到底部
- [ ] 5.4 实现流式输出完成后标记 isStreaming = false

## 6. 消息操作

- [ ] 6.1 实现 hover 显示操作按钮栏（根据消息类型显示不同按钮）
- [ ] 6.2 实现复制消息（复制 Markdown 原始内容到剪贴板）
- [ ] 6.3 实现重新发送（用户消息原样重发）
- [ ] 6.4 实现编辑并重发（inline 编辑框 + 清除后续消息 + 重新请求 AI）
- [ ] 6.5 实现重新生成（删除当前 AI 回复 + 重新请求）
- [ ] 6.6 实现停止生成（中断 setInterval，保留已输出内容）

## 7. 数据层

- [ ] 7.1 填充 useChatStore 完整实现（sessions CRUD、messages CRUD、currentSessionId）
- [ ] 7.2 实现消息按会话分片存储（每个 session 的 messages 独立存储 key）
- [ ] 7.3 实现会话切换时懒加载消息数据

## 8. 验证

- [ ] 8.1 验证新建会话 → 输入消息 → 发送 → Mock 流式回复 → 完整渲染链路
- [ ] 8.2 验证 Markdown 渲染（标题、列表、代码块、表格均正确）
- [ ] 8.3 验证会话管理（新建、切换、重命名、删除、置顶、导出）
- [ ] 8.4 验证 ⌘K 搜索功能
- [ ] 8.5 验证消息操作（复制、重发、编辑重发、重新生成、停止）
- [ ] 8.6 验证深色/浅色主题下聊天页视觉正确
